@model IEnumerable<Vidly.Models.Customer>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Customers</h1>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Customer</th>
        </tr>
    </thead>
    @if (Model.Any())
    {

        foreach (var cus in Model)
        {
            <tbody>
                <tr>
                    <td>
                        @Html.ActionLink(cus.CustomerName, "Details", "Customers", new { id = cus.CustomerId }, null)
                    </td>
                </tr>
            </tbody>
        }
    }
    else
    {
        <tbody>
            <tr>
                <td>No Cutomers in database</td>
            </tr>
        </tbody>
    }
</table>
